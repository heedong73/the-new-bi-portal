/**
 * 대시보드 (`/`).
 *
 * 후속 task에서 핵심 지표 요약 화면으로 확장된다. 현재는 스텁.
 */
import ko from "@/i18n/ko";
import PageStub from "./PageStub";

export default function DashboardPage() {
  return <PageStub title={ko.pages.dashboard} />;
}
