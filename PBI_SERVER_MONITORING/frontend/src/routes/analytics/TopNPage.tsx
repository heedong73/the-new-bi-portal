/**
 * Top N 분석 (`/analytics/top-n`).
 *
 * 후속 task에서 가장 오래/자주 실패한 리포트 Top N 분석으로 확장된다. 현재는 스텁.
 */
import ko from "@/i18n/ko";
import PageStub from "../PageStub";

export default function TopNPage() {
  return <PageStub title={ko.pages.topN} />;
}
